interface:
  display_name: "E Vara"
  short_description: "Repo-specific patterns and workflows for E-vara"
  default_prompt: "Use the E-vara repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true